# ========================================
# 로컬 환경 예제 파일 (local.properties.example)
# ========================================

# 안드로이드 SDK 경로
sdk.dir=/path/to/your/android/sdk

# NDK 경로 (필요시)
ndk.dir=/path/to/your/android/ndk

# Gradle 캐시/빌드 관련 설정 (선택)
# org.gradle.daemon=true
# org.gradle.parallel=true

# 주의: 실제 사용 시 이 파일을 local.properties로 복사 후 환경에 맞게 수정
